Принято, Леонид. Формулирую **TASK 15** строго по протоколу **D=>C (Dashka → Claude)** — без воды, с чёткими целями, входными данными и ожидаемым результатом. Это именно то ТЗ, которое ты сейчас проговорил голосом и показал записью экрана.

---

# 🟦 **TASK 15 — Database Integration & Persistence Fix**

**Назначение:** Claude (AI Engineer)
**Проект:** Dashka Record / Solar Recorder
**Приоритет:** HIGH
**Статус:** OPEN
**Связанные проблемы:**

* Запись экрана создаётся и отображается (WebM preview есть)
* После перезагрузки / перехода в Library — **пусто**
* Данные **не сохраняются в БД**
* Vercel deployment падает с `routes-manifest.json not found`

---

## 🎯 ЦЕЛЬ ЗАДАЧИ

1. **Проверить и восстановить сохранение записей в базе данных**
2. **Обеспечить, что каждая запись экрана:**

   * сохраняется в storage (файл)
   * регистрируется в БД (metadata)
   * отображается в `/records` после перезагрузки
3. **Исправить конфигурацию деплоя на Vercel**
   (или чётко зафиксировать, почему Vercel не подходит для данного backend-профиля)

---

## 🧩 ТЕКУЩАЯ ФАКТИЧЕСКАЯ СИТУАЦИЯ (as-is)

### Работает:

* 🎥 Запись экрана (MediaRecorder)
* 📦 Upload API (`/api/upload`)
* 👀 Preview WebM после записи
* ⚙️ Background processing запускается

### НЕ работает:

* ❌ Запись **не появляется** в `/records`
* ❌ После обновления страницы запись пропадает
* ❌ File manager пустой
* ❌ Нет persistence слоя (DB)
* ❌ Vercel build error (`routes-manifest.json`)

---

## 🔍 ЗАДАЧИ ДЛЯ АНАЛИЗА (ОБЯЗАТЕЛЬНО)

### 1️⃣ Проверка persistence логики

* Найти, **где именно должна происходить запись в БД**
* Проверить:

  * `lib/storage.ts`
  * `createMetadata()`
  * `readMetadata()`
* Ответить:

  * используется ли **реальная БД** или file-based JSON
  * вызывается ли запись в БД после upload
  * есть ли race condition (process async, metadata не успевает)

---

### 2️⃣ Проверка `/records` страницы

* Откуда `/records` берёт данные?
* Какой API вызывается?
* Почему возвращается пустой список?
* Проверить:

  * `/api/files`
  * `/api/files/[id]`
* Убедиться, что:

  * backend реально возвращает список
  * frontend корректно его отображает

---

### 3️⃣ База данных — WHAT & HOW

Claude должен:

* Предложить **конкретный вариант БД**:

  * SQLite (dev)
  * PostgreSQL / Supabase (prod)
* Подготовить:

  * схему таблицы `recordings`
  * поля (id, filename, duration, created_at, status, paths, etc.)
* Показать:

  * где и как происходит `INSERT`
  * где `SELECT` для `/records`

> ⚠️ Важно: **не теоретически**, а привязано к текущему коду проекта.

---

### 4️⃣ Vercel — архитектурный вердикт

Claude должен:

* Проанализировать, **почему Vercel падает**
* Учитывая:

  * Node APIs
  * FFmpeg
  * filesystem
  * background processing
* Дать **чёткий вывод**:

  * либо точный fixed config для Vercel
  * либо аргументированный вывод:
    **“Vercel не подходит → использовать Render / VPS”**

Без общих фраз. С фактами.

---

## 📦 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ (TO-BE)

Claude должен вернуть:

1. 📄 **Краткий отчёт**:

   * где именно сейчас ломается сохранение
   * почему `/records` пустой
2. 🧱 **Предложенную DB-архитектуру**
3. 🧩 **Конкретные изменения в коде** (файлы + фрагменты)
4. 🚀 **Рекомендацию по деплою**:

   * Vercel (если реально)
   * или окончательный переход на Render/VPS

---

## ✅ КРИТЕРИЙ ПРИЁМКИ (ACCEPTANCE CRITERIA)

* Записал экран
* Перешёл в `/records`
* Обновил страницу
* 👉 **Запись на месте**
* 👉 Метаданные сохранены
* 👉 Поведение одинаково локально и в проде

---

## 🧭 КОНТЕКСТ ОТ АРХИТЕКТОРА (Leanid)

> Мы записываем экран → видео есть → preview есть →
> но из-за отсутствия базы данных запись не сохраняется.
> Нужно довести продукт до состояния **реального persistence**,
> и закрыть вопрос с Vercel (либо починить, либо честно отказаться).

---

### 🟢 **D=>C**

Claude, приступай к **TASK 15**.
Работаем по фактическому коду, без абстракций.
task15